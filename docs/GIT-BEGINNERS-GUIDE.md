# Git и CI/CD: Гайд для начинающих

Этот документ объясняет основы Git и CI/CD для тех, кто только начинает работать с проектом.

## Часть 1: Что такое Git?

### Простыми словами

Git — это система контроля версий. Представь Google Docs, где видна история изменений документа. Git делает то же самое, но для кода.

**Зачем нужен:**
- Сохранять историю всех изменений
- Работать нескольким людям над одним проектом
- Откатываться к предыдущим версиям
- Экспериментировать без страха сломать рабочий код

### Основные понятия

```
Repository (репозиторий)
└── Папка с проектом + вся история изменений

Commit (коммит)
└── "Снимок" состояния проекта в определённый момент
    Как сохранение в игре

Branch (ветка)
└── Параллельная линия разработки
    Можно экспериментировать, не трогая основной код

Remote (удалённый репозиторий)
└── Копия репозитория на сервере (GitHub)
    Для синхронизации между разработчиками
```

### Наш Git workflow

```
main (продакшен)     ←── Сюда попадает только проверенный код
  │                      Отсюда автоматически деплоится на сервер
  │
  └── dev (разработка) ←── Здесь ведётся основная работа
        │
        └── feature/xxx ←── Отдельные фичи разрабатываются в своих ветках
```

## Часть 2: Основные команды Git

### Начало работы

```bash
# Склонировать репозиторий (скачать проект)
git clone https://github.com/marcus-kit/pg19.git

# Перейти в папку проекта
cd pg19

# Установить зависимости
pnpm install
```

### Ежедневная работа

```bash
# Проверить статус (какие файлы изменены)
git status

# Получить последние изменения с сервера
git pull

# Посмотреть историю коммитов
git log --oneline -10
```

### Создание коммита

```bash
# 1. Посмотреть что изменилось
git status

# 2. Добавить файлы в коммит
git add .                    # Все файлы
git add apps/landing/        # Или конкретную папку

# 3. Создать коммит с описанием
git commit -m "feat: добавил форму регистрации"

# 4. Отправить на сервер
git push
```

### Работа с ветками

```bash
# Посмотреть все ветки
git branch -a

# Переключиться на ветку
git checkout dev

# Создать новую ветку и переключиться
git checkout -b feature/new-login

# После завершения работы — слить в dev
git checkout dev
git merge feature/new-login
```

## Часть 3: Что такое CI/CD?

### Простыми словами

**CI (Continuous Integration)** — автоматическая проверка кода при каждом изменении.

**CD (Continuous Deployment)** — автоматический деплой на сервер после успешных проверок.

```
Ты делаешь push
      │
      ▼
┌─────────────────────────────────────────────┐
│            GitHub Actions (CI)              │
│                                             │
│  1. Скачивает код                          │
│  2. Устанавливает зависимости              │
│  3. Проверяет TypeScript                   │
│  4. Запускает тесты (если есть)            │
│                                             │
│  Всё ок? ──────────────────────────────┐   │
│                                         │   │
│                                         ▼   │
│                          ┌──────────────────┤
│                          │   Deploy (CD)    │
│                          │                  │
│                          │  Vercel собирает │
│                          │  и публикует     │
│                          │  приложение      │
│                          └──────────────────┤
└─────────────────────────────────────────────┘
      │
      ▼
Сайт обновлён автоматически!
```

### Как это работает в нашем проекте

1. Ты пушишь код в ветку `main`
2. GitHub видит изменения и запускает workflow
3. Workflow проверяет TypeScript во всех приложениях
4. Если проверки прошли — деплоит на Vercel
5. Через 2-3 минуты изменения видны на сайте

### Где смотреть статус

**GitHub Actions:**
https://github.com/marcus-kit/pg19/actions

Там видно:
- Зелёная галочка ✅ — всё успешно
- Красный крестик ❌ — что-то сломалось
- Жёлтый кружок 🟡 — в процессе

## Часть 4: Практические сценарии

### Сценарий 1: Исправить баг

```bash
# 1. Убедись что на актуальной версии
git checkout main
git pull

# 2. Внеси изменения в код
# ... редактируешь файлы ...

# 3. Проверь что работает локально
pnpm dev

# 4. Закоммить и запушить
git add .
git commit -m "fix: исправил отображение цен"
git push

# 5. Жди 2-3 минуты — изменения появятся на сайте
```

### Сценарий 2: Разработка новой фичи

```bash
# 1. Создай ветку для фичи
git checkout main
git pull
git checkout -b feature/user-profile

# 2. Работай над фичей
# ... несколько дней разработки ...
git add .
git commit -m "feat: добавил страницу профиля"

# 3. Когда готово — создай Pull Request
git push -u origin feature/user-profile
# Затем на GitHub создай Pull Request

# 4. После ревью — merge в main
# Это можно сделать на GitHub
```

### Сценарий 3: Что делать если CI упал

```bash
# 1. Посмотри логи ошибки
gh run view --log-failed

# 2. Исправь ошибку в коде

# 3. Закоммить исправление
git add .
git commit -m "fix: исправил ошибку TypeScript"
git push

# CI автоматически перезапустится
```

## Часть 5: Правила хорошего тона

### Коммиты

Используй понятные сообщения:

```bash
# Хорошо
git commit -m "feat: добавил авторизацию через Google"
git commit -m "fix: исправил падение при пустом email"
git commit -m "docs: обновил README"

# Плохо
git commit -m "fix"
git commit -m "изменения"
git commit -m "111"
```

Префиксы:
- `feat:` — новая функциональность
- `fix:` — исправление бага
- `docs:` — документация
- `style:` — форматирование (пробелы, запятые)
- `refactor:` — рефакторинг без изменения поведения
- `test:` — добавление тестов

### Ветки

```bash
# Называй ветки понятно
feature/user-auth      # Новая фича
fix/login-error        # Исправление бага
hotfix/critical-bug    # Срочное исправление
```

### Перед push

1. Проверь что код работает локально: `pnpm dev`
2. Проверь TypeScript: `pnpm typecheck`
3. Проверь что не добавил лишнего: `git status`

## Часть 6: Структура нашего проекта

```
pg19/
├── apps/                    # Приложения
│   ├── client-portal/       # Портал для клиентов
│   ├── admin-panel/         # Админка
│   ├── landing/             # Лендинг v1
│   └── landingv2/           # Лендинг v2
│
├── packages/                # Общий код
│   ├── ui/                  # UI компоненты
│   ├── shared/              # Общие утилиты
│   └── config/              # Конфигурации
│
├── .github/
│   └── workflows/
│       └── deploy.yml       # CI/CD конфигурация
│
└── docs/                    # Документация
    ├── CI-CD-SETUP.md
    └── GIT-BEGINNERS-GUIDE.md  # Этот файл
```

## Часть 7: Полезные ссылки

- **GitHub репозиторий:** https://github.com/marcus-kit/pg19
- **GitHub Actions:** https://github.com/marcus-kit/pg19/actions
- **Vercel Dashboard:** https://vercel.com/vts-projects-d91d9265

### Production URLs

- Client Portal: https://pg19-client.vercel.app
- Admin Panel: https://pg19-admin.vercel.app
- Landing: https://pg19-land.vercel.app
- Landing V2: https://pg19-landv2.vercel.app

## Часть 8: FAQ

**Q: Я что-то сломал, как откатить?**
```bash
# Отменить последний коммит (изменения останутся в файлах)
git reset HEAD~1

# Или полностью вернуться к последней версии с сервера
git reset --hard origin/main
```

**Q: Как посмотреть что изменилось в файле?**
```bash
git diff path/to/file.vue
```

**Q: Как узнать кто и когда менял файл?**
```bash
git blame path/to/file.vue
```

**Q: Конфликт при merge — что делать?**
1. Открой файл с конфликтом
2. Найди маркеры `<<<<<<<`, `=======`, `>>>>>>>`
3. Выбери нужную версию или объедини
4. Удали маркеры
5. `git add .` и `git commit`

**Q: Почему CI упал?**
1. Зайди на https://github.com/marcus-kit/pg19/actions
2. Найди упавший workflow
3. Кликни на красный джоб
4. Читай логи ошибки

---

Если остались вопросы — спрашивай в команде!
